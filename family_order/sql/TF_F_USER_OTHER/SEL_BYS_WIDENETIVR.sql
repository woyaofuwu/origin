SELECT nvl(A.RSRV_STR2/100,0)
  FROM TF_F_USER_OTHER A
 where 1 = 1
   AND a.USER_ID = :USER_ID
   AND a.PARTITION_ID = MOD(to_number(:USER_ID), 10000)
   AND A.RSRV_VALUE_CODE = 'FTTH'
   AND SYSDATE BETWEEN A.START_DATE AND A.END_DATE