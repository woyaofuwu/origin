SELECT A.TRADEID APPLY_NO,
       A.ICCID,
       A.IMSI,
       A.KI,
       A.K,
       A.OPC,
       A.SMSP,
       A.PIN1,
       A.PIN2,
       A.PUK1,
       A.PUK2,
       A.ACC,
       A.ADM,
       A.DEAL_TAG
  FROM TI_IOT_CARDDATAFILE A
  WHERE 1=1
  AND  A.IMSI = :IMSI