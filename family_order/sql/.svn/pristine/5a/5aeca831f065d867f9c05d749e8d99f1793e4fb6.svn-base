SELECT TO_CHAR(TRADE_ID) TRADE_ID,
       TO_CHAR(ORDER_ID) ORDER_ID,
       TO_CHAR(BPM_ID) BPM_ID,
       TRADE_TYPE_CODE,
       IN_MODE_CODE,
       PRIORITY,
       SUBSCRIBE_STATE,
       NEXT_DEAL_TAG,
       PRODUCT_ID,
       BRAND_CODE,
       TO_CHAR(USER_ID) USER_ID,
       TO_CHAR(CUST_ID) CUST_ID,
       TO_CHAR(ACCT_ID) ACCT_ID,
       SERIAL_NUMBER,
       CUST_NAME,
       TO_CHAR(ACCEPT_DATE, 'YYYY-MM-DD HH24:MI:SS') ACCEPT_DATE,
       ACCEPT_MONTH,
       TRADE_STAFF_ID,
       TRADE_DEPART_ID,
       TRADE_CITY_CODE,
       TRADE_EPARCHY_CODE,
       TERM_IP,
       EPARCHY_CODE,
       CITY_CODE,
       OLCOM_TAG,
       TO_CHAR(EXEC_TIME, 'YYYY-MM-DD HH24:MI:SS') EXEC_TIME,
       TO_CHAR(FINISH_DATE, 'YYYY-MM-DD HH24:MI:SS') FINISH_DATE,
       TO_CHAR(OPER_FEE) OPER_FEE,
       TO_CHAR(FOREGIFT) FOREGIFT,
       TO_CHAR(ADVANCE_PAY) ADVANCE_PAY,
       INVOICE_NO,
       FEE_STATE,
       TO_CHAR(FEE_TIME, 'YYYY-MM-DD HH24:MI:SS') FEE_TIME,
       FEE_STAFF_ID,
       CANCEL_TAG,
       TO_CHAR(CANCEL_DATE, 'YYYY-MM-DD HH24:MI:SS') CANCEL_DATE,
       CANCEL_STAFF_ID,
       CANCEL_DEPART_ID,
       CANCEL_CITY_CODE,
       CANCEL_EPARCHY_CODE,
       PROCESS_TAG_SET,
       RSRV_STR1,
       RSRV_STR2,
       RSRV_STR3,
       RSRV_STR4,
       RSRV_STR5,
       RSRV_STR6,
       RSRV_STR7,
       RSRV_STR8,
       RSRV_STR9,
       RSRV_STR10,
       REMARK
  FROM TF_BH_TRADE A
 WHERE A.TRADE_ID = (SELECT /*+ ORDERED INDEX(B IDX_TF_BH_TRADE_USERID)*/
                    MAX(B.TRADE_ID)
                     FROM TF_BH_TRADE B
                    WHERE B.USER_ID = TO_NUMBER(:USER_ID)
                      AND B.CANCEL_TAG = '0'
                      AND (B.TRADE_TYPE_CODE = :TRADE_TYPE_CODE OR
                          :TRADE_TYPE_CODE IS NULL))