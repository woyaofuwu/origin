SELECT P.SUBORDER_ID,
       P.GOODS_ID,
       P.PRODUCT_ID,
       P.PRODUCT_TYPE,
       P.SERVICEID_LIST,
       P.RSRV_STR1,
       P.RSRV_STR2,
       P.RSRV_STR3,
       P.RSRV_STR4,
       P.RSRV_STR5,
       S.SUBEXTORDER_ID,
       S.ORDER_ID,
       S.STATE,
       S.DEAL_STATE,
       S.DEAL_DESC,
       S.NUMBER_OPRTYPE,
       S.SERVICENO,
       S.SERVICENO_TYPE,
       S.NUMBER_BRAND,
       S.QUANTITY,
       S.NUMBER_PRICE,
       S.SIM_PRICE,
       S.LEGAL_NAME,
       S.CERTIFICATE_TYPE,
       S.CERTIFICATE_NO,
       S.ACTIVITY_CODE,
       S.ACTIVITY_TYPE,
       S.ACTIVITY_NAME,
       S.ACTIVITY_DESC,
       S.SUBTOTAL_FEE,
       S.ADJUST_FEE,
       S.ADJUST_REASON,
       S.ORDER_STATUS,
       S.GOODS_TITLE,
       S.AMOUNT,
       S.PRICE,
       S.GOODS_PROVINCE,
       S.GOODS_CITY,
       C.CONTRACT_ID,
       C.BOSS_ID,
       C.CONTRACT_TYPE,
       C.CONTRACT_CONTENT,
       C.CONTRACT_NAME,
       C.CONTRACT_AREA,
       C.TOTAL_PRICE,
       C.PRE_PRICE,
       C.GOODS_PRICE,
       C.DEPOSIT_FEE,
       C.GRADE_LOW_LIMIT,
       C.PROMISE_DURATION,
       C.RETURN_MONTH,
       C.EFFECT_TIME,
       C.RETURN_RULE,
       C.PRESENT_RULE,
       C.DEMO,
       C.STATUS
  FROM TF_B_CTRM_GERLPRODUCT  P,
       TF_B_CTRM_GERLSUBORDER S,
       TD_B_CTRM_CONTRACT     C
 WHERE P.SUBORDER_ID = S.SUBORDER_ID
   AND P.PRODUCT_ID = C.CONTRACT_ID
   AND P.SUBORDER_ID = :SUBORDER_ID
