SELECT COUNT(1) 活跃数
   FROM TF_SM_BI_VGOP T
  WHERE T.SERIAL_NUMBER = :MOBILE_NUM
    AND T.OPER_CODE = :OPER_CODE
    AND T.VGOP_TIME >= TO_CHAR(TRUNC(ADD_MONTHS(SYSDATE, -1), 'MM'), 'YYYYMMDD') --发放积分的上月有客户端活动记录