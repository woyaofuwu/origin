select month,
       user_id,
       serial_number,
       deal_flag,
       accept_date,
       prize_type_code,
       exec_flag,
       exec_time
  from tf_f_user_lottery
where 1=1
  and user_id = :USER_ID 
  and month = :MONTH