--IS_CACHE=Y
SELECT * 
FROM TD_B_PRODUCTMODULE_ACTION
        WHERE STATE = '1'
        AND (ELEMENT_TYPE_CODE = :ELEMENT_TYPE_CODE OR ELEMENT_TYPE_CODE = '-1')
        AND (ELEMENT_ID = :ELEMENT_ID OR ELEMENT_ID = '-1')
        AND (TRADE_TYPE_CODE = :TRADE_TYPE_CODE OR TRADE_TYPE_CODE = '-1')
        AND (ORDER_TYPE_CODE = :ORDER_TYPE_CODE OR ORDER_TYPE_CODE = '-1')
        AND (IN_MODE_CODE = :IN_MODE_CODE OR IN_MODE_CODE = '-1')
        ORDER BY ORDER_NO ASC
