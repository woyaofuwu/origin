SELECT A.*
  FROM TF_F_TERMINALORDER A
 WHERE A.SERIAL_NUMBER = :SERIAL_NUMBER
   AND A.ORDER_ID = :ORDER_ID
   AND A.RSRV_STR2 = :RSRV_STR2
   AND A.ORDER_STATE = :ORDER_STATE
   AND A.END_TIME > SYSDATE
 ORDER BY A.START_TIME DESC
